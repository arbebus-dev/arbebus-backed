Arbebus PRO MAX package

Included important fixes:
- backend/server.js now exposes /transit/plan, /live-buses, /news, /push/register, /leave-alerts on the real backend entrypoint
- /transit/plan now uses backend/services/transit/planner/plannerController.js
- /live-buses now returns an array compatible with hooks/useLiveBuses.ts
- backend/config/env.js loads .env, backend/.env and app/.env
- Added MAX_WALKING_METERS, MAX_TRANSFERS, REDIS_URL, LEAVE_ALERT_ENGINE_INTERVAL_MS env reads
- Added missing backend/services/transit/stopMatcher.js
- plannerService now uses MAX_WALKING_METERS and MAX_TRANSFERS, returns up to 3 options, and enriches first option with live ETA when possible
- core/services/transit/plannerApi.ts return type fixed to include meta
- components/home/HomeMapLayer.tsx upgraded to show pickup/destination markers, dual route polyline and driver dashed route
- app/(tabs)/index.tsx aligned with HomeMapLayer pickup props
- package.json now includes ioredis and node-fetch
- Added placeholder assets required by Expo/app build

After replacing files:
1. npm install
2. node backend/server.js
3. git add .
4. git commit -m "Arbebus PRO MAX alignment"
5. git push

Check Render env:
DATABASE_URL=...
REDIS_URL=redis://...
MAX_WALKING_METERS=500
MAX_TRANSFERS=3
ENABLE_CORS=true
CORS_ORIGIN=*
