// GPS forwarding example for Arbebus search.service.js

// Replace local_address provider call with:

/*
runProvider(
  "local_address",
  () =>
    searchLocalAddresses(q, {
      limit: Math.max(8, limit),
      lat: query.lat ?? query.latitude,
      lon: query.lon ?? query.lng ?? query.longitude,
    }),
  15000,
),
*/