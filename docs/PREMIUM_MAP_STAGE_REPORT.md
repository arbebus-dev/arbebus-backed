# Premium Map Stage Report

Implemented in this package:

- Smooth active-trip camera follow retained and wired to selected route / vehicle.
- Station access endpoint added: `/api/transit/station-access?stopId=`.
- `backend/src/data/stations/entrances.json` added with fallback model.
- Mobile station access API + map layer added.
- Animated selected bus pulse added.
- GTFS-RT fallback env key documented: `GTFS_RT_TRIP_UPDATES_URL=`.
- App Store privacy docs added in LT/EN.
- Render / EAS deploy checklist added.

Copy only the files listed in the chat response unless you intentionally want to replace the full project snapshot.
