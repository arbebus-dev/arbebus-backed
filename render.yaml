services:
  - type: web
    name: arbebus-backend
    runtime: node
    plan: starter
    buildCommand: npm install
    startCommand: node backend/server.js
    envVars:
      - key: HOST
        value: 0.0.0.0
      - key: PORT
        value: 10000
      - key: ENABLE_CORS
        value: "true"
